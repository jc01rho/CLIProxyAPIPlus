# OmniRoute Compatibility Matrix

Frozen comparison source: OmniRoute commit `ed2db6cb19ba534980c5b3e046501e8a5c40c458`. Local baseline reviewed against commit `5d4bff6a45f467783750d0d36a435a8b2def88e7` plus the independently implemented changes identified below. Scope is exactly G1-01 through G1-09; additions require a scope amendment.

| ID | Direction | OmniRoute path / symbol | Local path / symbol / fixture | Final disposition | Provenance | Status and sign-off |
|---|---|---|---|---|---|---|
| G1-01 | Responses → Chat | `open-sse/translator/request/openai-responses.ts` / `openaiResponsesToOpenAIRequest` | `internal/translator/openai/openai/responses/openai_openai-responses_request.go` / `ConvertOpenAIResponsesRequestToOpenAIChatCompletions`; `TestConvertOpenAIResponsesRequestToOpenAIChatCompletions_NormalizesLeadingDeveloper`, `TestConvertOpenAIResponsesRequestToOpenAIChatCompletions_KeepsLaterDeveloper` | **adapt**: ordered leading instructions use the shared normalizer; later developer role/content remains at its conversational index. Existing tool adjacency, reasoning, namespace, and image-detail behavior is retained. | Independently implemented from the approved semantic contract; comparative citation only. | Complete; implementation and focused test evidence recorded. |
| G1-02 | Chat → Responses | `open-sse/translator/request/openai-responses/toResponses.ts` / `openaiToOpenAIResponsesRequest` | Local Chat/Responses registry routes and `internal/translator/openai/openai/chat-completions/openai_openai_request.go` / `ConvertOpenAIRequestToOpenAI`; existing Responses request/response fixtures cover tools, namespaces, images/files, reasoning, and structured output. | **already equivalent** for the bounded audit: no concrete red request fixture established a missing local conversion requiring product change. | Comparative audit only. | Complete; no change authorized; translator review sign-off recorded. |
| G1-03 | Chat → Claude | `open-sse/translator/request/openai-to-claude.ts` / `openaiToClaudeRequest` | `internal/translator/claude/openai/chat-completions/claude_openai_request.go` / `ConvertOpenAIRequestToClaude`; `TestConvertOpenAIRequestToClaude_LeadingDeveloperBecomesTopLevelSystem`, `...LaterDeveloperPreservedAsUserInPlace`, `...LeadingNonTextDeveloperIsNotSilentlyDropped`, `...UnsupportedSystemPartIsNotSilentlyDropped` | **adapt**: only string/text-block instruction prefixes are normalized to Claude top-level text blocks; non-text instruction content is retained in a deterministic in-position user compatibility block instead of partially hoisted or dropped. Tool/media IDs, cache control, and fallback turn remain local behavior. | Independently implemented; OmniRoute string-flattening code was not copied or adapted. | Complete; implementation and focused test evidence recorded. |
| G1-04 | Claude → Chat | `open-sse/translator/request/claude-to-openai.ts` / `claudeToOpenAIRequest` | `internal/translator/openai/claude/openai_claude_request.go` / `ConvertClaudeRequestToOpenAI`; existing package fixtures cover cache blocks, attribution filtering, signed thinking, schemas, and tool results. | **local superior**: preserve local cache/content arrays, attribution filtering, signature validation, schema normalization, and parallel tool-result handling. No bounded red fixture found. | Comparative audit only. | Complete; rejected migration; translator review sign-off recorded. |
| G1-05 | Claude → Responses | `open-sse/translator/request/claude-to-openai.ts` / `claudeToOpenAIRequest` (Responses-target behavior) | `internal/translator/claude/openai/responses/claude_openai-responses_request.go` / `ConvertOpenAIResponsesRequestToClaude` is the local direct opposite pivot; Claude-to-Responses behavior is provided through local registered composition and response translators; existing Claude Responses fixtures cover tools, cache, thinking/signatures, and response identity. | **reject** direct port: no concrete red fixture established a missing direct local request pivot, while local signature and response-identity rules are mandatory. | Comparative audit only. | Complete; no change authorized; translator review sign-off recorded. |
| G1-06 | Responses → Claude | OmniRoute composition: `openai-responses.ts` / `openaiResponsesToOpenAIRequest`, then `openai-to-claude.ts` / `openaiToClaudeRequest` | Local composition of `ConvertOpenAIResponsesRequestToOpenAIChatCompletions` and `ConvertOpenAIRequestToClaude`; composition asserted by `TestConvertOpenAIResponsesRequestToOpenAIChatCompletions_KeepsLaterDeveloper`; direct implementation remains `internal/translator/claude/openai/responses/claude_openai-responses_request.go`. | **already equivalent** after G1-01/G1-03 adaptations: later developer content remains ordered and becomes an explicit Claude user compatibility block without hoisting; no new router or direct port. | Independent composition test and local implementations. | Complete; implementation and focused test evidence recorded. |
| G1-07 | Chat → Gemini | `open-sse/translator/request/openai-to-gemini.ts` / `openaiToGeminiBase` and registered exports | `internal/translator/gemini/openai/chat-completions/gemini_openai_request.go` / `ConvertOpenAIRequestToGemini`; `gemini_openai_request_test.go` fixtures cover empty parts, audio/video, reasoning order, tool responses/names, schemas, and structured output; Gemini CLI/Antigravity variants retain local handling. | **local superior**: local system/developer instruction handling, explicit tool identity, signature gates, multimedia, thinking, and schema cleanup exceed the compared behavior. No additional red fixture found. | Comparative audit only. | Complete; rejected migration; translator review sign-off recorded. |
| G1-08 | Gemini → Chat | `open-sse/translator/request/gemini-to-openai.ts` / `geminiToOpenAIRequest`, `splitCoLocatedFunctionResponses`, `convertGeminiContentWithReasoning` | `internal/translator/openai/gemini/openai_gemini_request.go` / `ConvertGeminiRequestToOpenAI`; fixtures `...FunctionResponsesConsumeToolCallIDsFIFO`, `...PreservesExplicitFunctionCallIDs`, `...AcceptsSnakeInlineData`, `...SplitsNonImageInlineDataByMIME`. | **local superior**: preserve explicit/FIFO tool IDs, thought separation, and inline-data handling; reject random-ID migration. No bounded red fixture found. | Comparative audit only. | Complete; rejected migration; translator review sign-off recorded. |
| G1-09 | Gemini ↔ Responses | OmniRoute Gemini↔Chat translators composed through its Responses hub | `internal/translator/gemini/openai/responses/gemini_openai-responses_request.go` / `ConvertOpenAIResponsesRequestToGemini` and local registered reverse/response paths; package fixtures cover namespace/tool identity, reasoning/signature carriers, multimedia, and stream/non-stream synthesis. | **local superior**: retain direct local Responses pivot, signature carrier, tool pairing/ordering, multimedia, and synthesis behavior. No concrete red fixture found. | Comparative audit only. | Complete; rejected migration; translator review sign-off recorded. |

## Licensing and provenance sign-off

No OmniRoute source code was copied or substantially adapted. G1-01 and G1-03 are independent local implementations guided by local failing fixtures and the approved semantic contract; all other rows are comparative audits resulting in already-equivalent, local-superior, or reject dispositions. Therefore no third-party notice is required for this change.
